# ==========================================
# WIN32 + PYTHON 3.11 (hackmu)
# Instalação em 2 fases:
#
# 1) Instalar pacotes "source" manualmente (sem --only-binary):
#    pip install PyMsgBox==2.0.1
#    pip install PyScreeze==1.0.1
#    pip install pyautogui==0.9.54
#
# 2) Depois instalar o restante (com wheels quando possível):
#    pip install --only-binary=:all: -r requirements.txt
#    (se algum falhar por wheel, instale ele separado sem --only-binary)
# ==========================================

# -------- Base (ok em win32) --------
keyboard==0.13.5
mss==10.0.0
Pillow==11.0.0
psutil==6.1.0
pydivert==2.1.0
pygetwindow==0.0.9
pyserial==3.5
pytesseract==0.3.13
screeninfo==0.8.1

# PyAutoGUI stack (INSTALAR ANTES - ver topo)
# pyautogui==0.9.54
# PyMsgBox==2.0.1
pyperclip==1.11.0
MouseInfo==0.1.3
# PyScreeze==1.0.1
pytweening==1.2.0
PyRect==0.2.0

# -------- NumPy/Matplotlib (compat win32) --------
numpy==1.26.4
matplotlib==3.7.5
pyparsing==3.2.0
packaging==24.2
six==1.17.0
wcwidth==0.2.13
prettytable==3.12.0

# -------- OpenCV --------
opencv-python==4.10.0.84

# -------- Utilitários --------
cffi==1.17.1
attrs==24.2.0
jinja2==3.1.4
setuptools==75.8.0

# -------- Web/API --------
requests==2.32.3
flask==3.1.0
python-dotenv==1.0.1
cloudscraper==1.2.71

# -------- Windows --------
pywin32==306

# -------- PDF (atenção) --------
# camelot-py tende a puxar pandas/ghostscript e quebrar no win32
# camelot-py==0.11.0
# tabula-py depende de Java instalado e configurado
tabula-py==2.10.0

# -------- Automação/Memory --------
pymem==1.14.0
pathfinding==1.0.17

# -------- Extras --------
pynput==1.7.7
discord.py==2.4.0
frida==17.2.11

# mitmproxy não roda bem em win32 (depende de mitmproxy_rs sem wheel)
# mitmproxy==11.0.2

# cryptography pode dar dor de cabeça em win32; mantenha só se precisar
cryptography==44.0.3

# FastAPI stack (ok) - sem uvicorn[standard] no win32
fastapi==0.115.6
uvicorn==0.32.1
pydantic==2.10.5